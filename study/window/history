https://www.w3schools.com/jsref/prop_loc_search.asp

[back]
<button onclick="history.back()">Go Back</button>
=
<button onclick="history.go(-1); return false;">Go Back</button>
history.back() is the same as history.go(-1).
 
[forward]
<button onclick="history.forward()">Go Forward</button>
=
<button onclick="history.go(1); return false;">Go Forward</button>

[go]
<button onclick="history.go(-2)">Go Back 2 Pages</button>
-The history.go() method loads a URL (page) from the history list.
-The history.go() method only works if the page exist in the history list.
history.go(0) reloads the page.
history.go(-1) is the same as history.back().
history.go(1) is the same as history.forward().

[length]
let length = history.length;
- Get the number of URLs in the history list
- 값은 최소 1 부터 시작 (현재 페이지를 포함하기 때문)
- 유저가 현재페이지까지 몇페이지를 걸쳐왔는지 확인할 때 유용하게 사용 가능함.
